# Disable PR bot comments — status checks (green/red) are sufficient
comment: false

# Configure Commit Status Checks (the green checkmark/red X list)
coverage:
  status:
    project:
      default:
        # Prevent "Informational" mode so a drop causes an actual red failure check
        informational: false
    patch:
      default:
        informational: false
